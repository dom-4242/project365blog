/**
 * DEPRECATED: sync-habits.ts
 *
 * Dieses Script hat Habits aus MDX-Frontmatter in die DailyHabits-Tabelle synchronisiert.
 * Seit Phase 1.5 sind Habits direkt in JournalEntry eingebettet und werden über den
 * Web-Editor erfasst. Dieses Script wird nicht mehr benötigt.
 */

console.log('sync-habits: deprecated since Phase 1.5 — habits are now stored in JournalEntry.')
console.log('New entries are created via the admin editor at /admin/entries/new.')
